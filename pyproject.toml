[project]
name = "open-meteo-weather-ingest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "openmeteo-requests>=1.7.5",
    "pandas>=2.3.3",
    "requests-cache>=1.3.3",
    "retry-requests>=2.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
